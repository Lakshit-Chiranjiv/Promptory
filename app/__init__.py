# This empty file tells Python that the `app` folder is a "package"
# — meaning other files can import from it using `from app.xxx import yyy`.
# Without this file, Python wouldn't recognize `app` as importable.
